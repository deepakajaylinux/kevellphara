============
Flirtify
============

Synopsis
----------------

This used to create phlagrant files for predefined templates. Non case sensitivity which brings added advantage for this module. This module have many alternative parameters. The default configuration management for PHP application.

Help command
------------------------

This help command guides the user to create phlagrant files for predefined templates. It has personalized  in phlagrant and dapperstrano. Also provides default  setting for virtual machine  configuration.
The help command for flirtify is shown below.

.. code-block:: bash

		phlagrant flirtify help

After inputs the above command, it starts functioning to enable the default setting. It catechesis the functions of flirtify in the 
screenshots.


.. code-block:: bash


 kevell@corp:/# phlagrant Flirtify help
 ******************************
 Pharaoh Tools - Phlagrant
 ******************************


  With Flirtify you can create Phlagrant files for your project from predefined templates. This will enable you to
  create default Virtual machine hardware and provisioning configurations for your project in a single command.

  Flirtify, flirt, flirtify, phlirt, phlirtify

        - default-cleo
        Create a Phlagrantfile for your project, with default Configuration Management for a PHP Application
        example: phlagrant flirt default-cleo

        - default-cleo-dapper
        Create a Phlagrantfile for your project, with default Configuration Management for a PHP Application and
        Dapperstrano Application
        example: phlagrant flirt default-cleo-dapper
            --host-dapperfile=*relative/path/to/dapperfile/"
            # guess will use build/config/dapperstrano/dapperfy/autopilots/generated/

        - custom-cleo-dapper
        Create a Phlagrantfile for your project
        example: phlagrant flirt custom-cleo-dapper

 ------------------------------
 End Help
 ******************************

Alternative parameters
-----------------------------------

There are many alternatives available in flirtify. They are,

Flirtify, Flirtify, flirt, phlirt, phlirtify

Default-cleo
------------------

This process used to create a phlagrant file for the user project with default configuration management for a PHP application. The following command is used to create a phlagrant file.

.. code-block:: bash

	phlagrant flirtify default-cleo

after input the above said command it starts functioning.

Custom-cleo-dapper
------------------------------

This process used to create a phlagrant file for user’s project. The following command is used to create a phlagrant file.

.. code-block:: bash
   
        phlagrant flirtify Custom-cleo-dapper

after input the above said command it starts functioning.

Default-cleo-dapper
----------------------------------

This process used to create a phlagrant file for the user project with default configuration management for a PHP application and dapperstrano application. The following command is used to create a phlagrant file. 

.. code-block:: bash

	phlagrant flirtify default-cleo-dapper

After input as the above said command it starts functioning.

Benefits
------------

* Well support for It professionals
* Non case sensitive
* Suitable for Ubuntu and Cent OS
* Used to create phlagrant files for predefined templates
